{
  "version": "0.2.0",
  "configurations": [


    {
      "type": "chrome", // Debug chrome
      "request": "launch", // VS Code has to launch the program
      "name": "Attach to localhost", // Just a name
      // "port": 9222, // Port number where debugging is enabled
      // Where are the files that are being served in the URL including the source maps
       "url": "http://localhost:3000/",
      "webRoot": "${workspaceFolder}",
      /* Override the URLs in source map which will be like localhost:port to chrome
         Port these to the .ts files in the project
         Use this only if you're having issues with source map not linking to js */
      // "sourceMapPathOverrides": {
      //   "./src/*": "${workspaceFolder}/src/*"
      // }
      // "debug.javascript.usePreview": false,
    //  "sourceMaps": true
    }
  ]
}